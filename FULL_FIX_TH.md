# รายการปรับแก้ฉบับผสานเต็ม

- ใช้ระบบ Neon PostgreSQL เป็นฐานข้อมูลหลัก
- ใช้ Vercel Blob แบบ Public สำหรับรูปภาพ
- ลบโค้ด Supabase และไฟล์ generator เก่าที่ไม่ใช้งาน
- ผสานค่า `.env.local` ที่ผู้ใช้ให้มาให้ครบ โดยไม่มีตัวแปรซ้ำ
- เพิ่ม `DATABASE_URL_UNPOOLED` สำหรับติดตั้งฐานข้อมูล
- เพิ่ม `services:check` เพื่อตรวจ Neon, AUTH_SECRET และ Vercel Blob
- ปรับ `db:setup` ให้สร้างตาราง ข้อมูลเริ่มต้น และบัญชีผู้ดูแลอัตโนมัติ
- แก้หน้าล็อกอินให้ข้อความ อีเมล รหัสผ่าน และ placeholder มองเห็นชัด
- ปิดการเปลี่ยนสีเว็บตาม Dark Mode ของ Windows เพื่อไม่ให้ช่องกรอกกลืนกับพื้นหลัง
- เพิ่มข้อความแจ้งข้อผิดพลาด Blob Store ให้เข้าใจง่าย
- เพิ่มไฟล์ Windows สำหรับติดตั้ง ตรวจระบบ ตั้งฐานข้อมูล รัน และ Build
- เพิ่มภาพ/โลโก้/วิดีโอ placeholder ตามชื่อไฟล์เดิมเพื่อไม่ให้หน้าเว็บมี asset แตก
- คงระบบจัดการสินค้า ราคา สี บทความ รีวิว ข้อมูลหน้าเว็บ และบัญชีผู้ดูแล
- คง Security Headers, HTTP-only cookie, bcrypt password hash และการตรวจสิทธิ์ Admin API
